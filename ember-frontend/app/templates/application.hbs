<div class="menu">
  {{#if session.isAuthenticated}}
    <p>
      Token: {{session.data.authenticated.access_token}} <button {{action "invalidateSession"}} >Logout</button>
    </p>
  {{else}}
    {{#link-to "login"}}Login{{/link-to}}
  {{/if}}
</div>
<div class="main">
  {{outlet}}
</div>